## apps/marketing (ceits.app) local environment
## Copy to `.env.local` if you want to override defaults.

# Workspace app origin (`use.ceits.app`). Used for Sign in / Create account links. Dev default: http://127.0.0.1:5174
# VITE_WORKSPACE_URL=https://use.ceits.app
