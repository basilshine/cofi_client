## apps/web local environment
## Copy to `.env.local` if you want to override defaults.

# Backend base URL.
# If omitted, the app uses `/api` and relies on Vite proxy (defaulting to http://127.0.0.1:8090).
VITE_API_URL=http://127.0.0.1:8090

